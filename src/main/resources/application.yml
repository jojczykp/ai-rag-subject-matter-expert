spring:
  application:
    name: ai-subject-matter-expert

aisme:
  documents:
    location: classpath:/subject-documents/

management:
  endpoints:
    web:
      exposure:
        include: health,info
