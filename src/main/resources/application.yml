spring:
  application:
    name: ai-subject-matter-expert
  main:
    banner-mode: console

aisme:
  documents:
    location: classpath:/subject-documents/
    chunk-size: 700
    chunk-overlap: 100

management:
  endpoints:
    web:
      exposure:
        include: health,info
