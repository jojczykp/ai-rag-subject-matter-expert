package org.alterbit.aisme.api

enum class ApiErrorCode {
    INVALID_REQUEST,
    MODEL_NOT_FOUND,
    MODEL_UNAVAILABLE,
    MODEL_CLIENT_NOT_FOUND,
    PROVIDER_TIMEOUT,
    PROVIDER_ERROR,
    INTERNAL_ERROR,
}
